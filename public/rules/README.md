# Bundled sing-box rule sets

These binary rule sets let subscription clients complete their first startup without reaching GitHub through a proxy that has not started yet.

Sources:

- `geosite-cn.srs`: `SagerNet/sing-geosite`, branch `rule-set`
- `geoip-cn.srs`: `SagerNet/sing-geoip`, branch `rule-set`
